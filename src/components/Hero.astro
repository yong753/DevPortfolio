---
// No props
---

<section class="hero">
    <h1>Jiyong yun</h1>
    <p class="role">Full-stack Developer</p>
    <p class="tagline">I build things for the web and learn by shipping.</p>
</section>

<style>
    .hero{
        max-width: : var(--max-width);
        margin: 0 auto;
        padding: var(--section-padding) var(--grid-gap);
        text-align: center;
    }

    h1{
        font-size: 2rem;
        font-weight: var(--font-weight-bold);
    }

    .role {
        color: var(--accent);
        font-size: 1.1rem;
        margin-top: 4px;
    }

    .tagline{
        color: var(--text-secondary);
        margin-top: 8px;
    }
</style>